# include <stdio.h>

void swap(int array[], int length);
int main(void) 
{
    return 0;

}

void swap(int array[], int length)
{
    for(int i = 0; i < length; i++)
    {
        printf("%d", array[i]);
    }
}
